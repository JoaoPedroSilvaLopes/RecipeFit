export type Usuario = {
  id: string;
  foto?: string;
  nome: string;
  email: string;
  peso: string;
  altura: string;
};

export type UsuarioFormInput = {
  foto?: string;
  nome: string;
  email: string;
  peso: string;
  altura: string;
};
