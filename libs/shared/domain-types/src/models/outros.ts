export type Usuario = {
  id: string;
  nome: string;
  email: string;
  peso: string;
  altura: string;
};

export type UsuarioFormInput = {
  nome: string;
  email: string;
  peso: string;
  altura: string;
};
