import {StyleSheet} from 'react-native';

export const styles = StyleSheet.create({
  textFieldGroup: {
    alignItems: 'center',
    gap: 10,
    width: '100%',
  },
  footerButtonGroup: {
    marginTop: 30,
    justifyContent: 'center',
    textAlign: 'center',
    flexDirection: 'row',
    gap: 30,
  },
});
